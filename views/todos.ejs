<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Huntober - Tasks</title>
    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="css/signup.css">
    <link rel="stylesheet" href="css/todos.css">

    <!-- Font-Awesome Icons -->
	<script src="https://kit.fontawesome.com/ab085a1591.js" crossorigin="anonymous"></script>
    
</head>
<body>

    <!-- ! DATE CALCULATIONS -->
    <%
    const date = new Date();
    // const finalDay = new Date("10/11/2022");
    // const differenceInTime = date.getDay() - finalDay.getDay()
    
    const now = new Date().getTime()
    const dueDate = new Date("10/11/2022")
    const difference = dueDate - now
    const daysLeft = Math.floor(difference / (1000 * 60 * 60 * 24))
    
    %>

    <div class="container">
        <div class="content">
            <header>
                <h1 class="header-text">The Hunt is On.</h1>

                <h2>Today is <%=date.toLocaleDateString() %></h2>

                <h2>There are <span class="bold"><%= daysLeft%></span> days until the <span class="bold">100 hours project</span> is due.</h2>

                <div class="hitlist-container">
                    <div class="hitlist-num">
                        <h2>There's <span class="bold"><%= %></span> to go for your <span class='bold'>hitlist</span>.</h2>
                    </div>
                    
                    <div class="hitlist-btn">
                        <button type="button" id="add">+</button>
                        <button type="button" id="minus">-</button>
                    </div>
                </div>
            </header>

            <div class="todos-wrapper">
                <div class="categories-wrapper">
                    <ul class="categories">
                        <!-- SAMPLE CATEGORIES... -->
                        <li class="category btn">Daily</li>
                        <li class="category btn">Weekly</li>
                        <li class="category btn">Big Picture</li>
                    </ul>
                </div>
                <table>
                    <thead>
                        <tr class="task-header">
                            <td>Task</td>
                            <td>Due Date</td>
                            <td>Category</td>
                            <td>Actions</td>
                        </tr>
                    </thead>
                    <tbody>
                        <!-- forEach - run through daysLeft function, sort, then forEach into EJS view -->
                        <% todos.sort((a, b) => a.dueDate - b.dueDate).forEach( el => { %>
                                <tr class="task-row">
                                    <!-- Todo Name -->
                                    <td data-id='<%= el._id %>'><span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.todo %></span></td>
                                    <!-- Todo Due Date -->
                                    <td data-id='<%= el._id %>'><span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= new Date(el.dueDate).toLocaleDateString() %></span></td>
                                    <!-- Todo Category (temp. won't be needed when category selection in place) -->
                                    <td data-id='<%= el._id %>'><span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.category %></span></td>
                                    <!-- Todo Actions (delete button, maybe more?) -->
                                    <td class='actions-cell' data-id='<%= el._id %>'><i class="fa-solid fa-trash-can del"></i></td>  
                                </tr>
                        <% }) %>  
                    </tbody>
                </table>  
            </div>
            
            <div>
                <h2 class="remaining-todos"><%= user.userName %> has <%= left %> things left to do.</h2>
            </div>
            

            <form action="/todos/createTodo" method='POST'>
                <h2>Add A Task:</h2>
                <div class="row form-row">
                    <label>
                        <span class="form-label">Task Name</span><input type="text" placeholder="Enter Todo Item" name='todoItem'>
                    </label>
                    
                </div>
                <div class="row form-row">
                    <label>
                        <span class="form-label">Due Date</span><input type="date" placeholder="Enter Due Date" name='todoDueDate'>
                    </label>
                    
                </div>
                <div class="row form-row">
                    <label>
                        <span class="form-label">Category</span><input type="text" placeholder="Enter Category" name='todoCategory'>
                    </label>
                    
                </div>
                <div class="row submit-row">
                    <input type="submit" class="btn submit-btn" value="Add Task">
                </div>
                
            </form>

       

            <div class="row submit-row">
                <a href="/logout" class="btn logout-btn">Logout</a>
            </div>
            
        </div>
    </div>
    
    
    <script src="js/main.js"></script>
</body>
</html>